# Server Configuration
PORT=3000

# Go Link Service Configuration
# Encryption key(s) for URL shortener/redirector service
# Generate with: node generate-go-link.mjs --generate-key
#
# SINGLE KEY (simplest, for new deployments):
# GO_LINK_SECRET_KEY=your_key_here
#
# MULTIPLE KEYS - COMMA SEPARATED (key rotation without expiration):
# GO_LINK_SECRET_KEY=new_key,old_key,older_key
#
# MULTIPLE KEYS - JSON FORMAT (with expiration dates):
# GO_LINK_SECRET_KEYS='[
#   {"key":"new_key_base64"},
#   {"key":"old_key_base64","expires":"2025-12-31T23:59:59Z"},
#   {"key":"older_key_base64","expires":"2024-12-31T23:59:59Z"}
# ]'
#
# KEY ROTATION & EXPIRATION STRATEGY:
# 1. New links always use the first key
# 2. Old links work with any listed key
# 3. Set expires date to auto-expire all links created with that key
# 4. Links generated after expires date will return 410 Gone
#
# EXPIRATION LOGIC:
# - If link.genDate > key.expires → Link expired (410)
# - If no expires date → Link never expires
# - Legacy links (no genDate) never expire
#
GO_LINK_SECRET_KEY=

# Optional: Override domain for link generation (defaults to dariomac.com)
# GO_LINK_DOMAIN=dariomac.com

# Optional: Override protocol for link generation (defaults to https)
# GO_LINK_PROTOCOL=https
